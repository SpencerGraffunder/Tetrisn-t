To run, you must have python 3 and pygame

```
pip install pygame
sudo apt-get install python-pil
```

then run main.py

```
python main.py
```

To create an executable, do

```
pyinstaller main.spec
```
